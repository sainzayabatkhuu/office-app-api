package com.sol.office_app.service;

import com.sol.office_app.common.GeneralService;
import com.sol.office_app.dto.UserDTO;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.security.Principal;
import java.util.Optional;

public interface UserService extends GeneralService<UserDTO, Long> {
//    LoginResponse refreshToken(Principal principal);

//    ProfileDTO me(Principal principal);

//    Optional<ProfileDTO> saveSettings(Principal principal, ProfileDTO profileDTO);

//    Optional<ChangePasswordDTO> changePassword(Principal principal, ChangePasswordDTO changePasswordDTO);

//    LoginDetailsForUserDTO loginHistory(Principal principal);

//    Page<UserRolesMappingDTO> userRoleMapping(Principal principal, Pageable pageable);

//    Page<BranchDTO> allowedBranches(Principal principal, Pageable pageable);
}
